# DevCPlusAi v1.0.2

正式维护版本，重点修复 Windows ZIP 解压兼容性和解压后 AI 界面启动失败的问题。

## 修复与改进

- ZIP 改用标准 Deflate 文件条目，移除旧打包方式生成的 `./` 根目录和 Unix 属性。发布前实际调用 Windows 自带压缩文件夹接口解压，并逐文件比对 SHA256。
- AI 聊天界面通过本地虚拟域名加载资源，避免中文、空格、`#`、`%`、`&` 等路径在文件 URL 转换过程中导致导航失败。
- 增加“页面脚本启动 → IDE 状态下发 → 页面确认”的双向握手；仅导航成功不再被当作界面启动成功，缺少脚本时会明确报错。
- 加强消息来源校验，只接收当前聊天页发出的宿主操作请求。
- 增加“复制界面诊断”菜单和故障状态下的诊断按钮，包含版本、启动路径和界面错误，不读取 API Key、用户配置或聊天记录。
- 应用标题和 EXE 文件属性统一显示 DevCPlusAi 1.0.2；诊断中单独标注底层 Dev-C++ 6.7.5。
- 打包时检查 EXE 版本与发布版本一致，并拒绝打入过期的网页资源。
- 新增中文 Windows 安装向导，自动创建开始菜单入口、可选桌面快捷方式，以及 Windows“已安装的应用”中的卸载项。
- 安装包内置 MinGW64、Node.js、Claude CLI 和微软签名的 WebView2 离线安装组件；检测到缺少 WebView2 时自动安装。
- 卸载只删除发布文件，保留用户源码、配置和聊天记录；升级及卸载前检查目标程序是否被占用。

## 下载与运行

- **推荐：`DevCPlusAi-1.0.2-windows-x64-gcc-setup.exe`**：完整安装包，双击并按向导安装，之后从开始菜单搜索 DevCPlusAi 启动。
- `DevCPlusAi-1.0.2-windows-x64-gcc.zip`：便携包，包含 MinGW64 编译器、Node.js、Claude CLI 和 AI 界面资源，完整解压后运行 `devcpp.exe`。
- 请选择一个包，解压到新的文件夹，保留整个目录结构。不要只复制 `devcpp.exe`，也不要直接在压缩包预览中运行。
- C/C++ 编译无需另配工具链；AI 对话需要自行配置服务商与 API Key。现代 AI 界面使用 Microsoft Edge WebView2 Runtime。
- 默认仅为当前 Windows 用户安装。配置保存在安装目录的 `config` 中，旧便携目录的个人配置不会自动迁移。
- 内置 GCC 8.1 工具链的安装目录不能包含 `%` 或 `#`；安装向导会检查此限制。AI 界面本身已覆盖这些路径的加载测试。

## 验证范围

发布流程覆盖原生构建与离线协议测试、聊天 Markdown/审批/滚动交互测试、特殊路径下真实 WebView2 双向握手、缺少脚本的失败检测、最终 ZIP 的 Windows 解压与文件哈希校验，以及解压目录中的真实面板和内置 C++ 编译运行。

本次为启动与发布可靠性修复。设置向导仍为原生窗口，整体设置界面的重新设计和更多 DPI/真实模型交互验收留待后续版本。
